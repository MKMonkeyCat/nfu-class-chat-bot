services:
  app:
    image: ghcr.io/mkmonkeycat/nfu-class-chat-bot:latest
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
