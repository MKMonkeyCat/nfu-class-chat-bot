apiVersion: v1
kind: Service
metadata:
  name: { { include "class-chat-bot.fullname" . } }
  labels: { { - include "class-chat-bot.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
      protocol: TCP
      name: http
  selector: { { - include "class-chat-bot.selectorLabels" . | nindent 4 } }
